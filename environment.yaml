# run: conda env create --file environment.yaml
name: panoptic
channels:
    - pytorch
    - conda-forge
    - anaconda

dependencies:
    - python>=3.9
    - pip
    - yacs
    - numpy
    - pytorch=1.10.0
    - torchvision
    - cudatoolkit=11.3
    - matplotlib
#    - opencv-python
#    - pyexr
    - plyfile
    - tqdm
    - pillow>=8.4.0
    - yacs>=0.1.6
    - scipy
    - opencv
    # - MinkowskiEngine # TODO
    # - pyMarchingCubes # TODO
    - pip:
      - -r requirements.txt
